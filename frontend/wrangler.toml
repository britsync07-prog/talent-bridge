name = "talent-bridge-frontend"
compatibility_date = "2024-04-01"
compatibility_flags = ["nodejs_compat"]

[pages_build_output_dir]
# This directory is where the build output will be stored
directory = ".vercel/output/static"
